<% content_for :body_content_left do %>
  <ul id="fieldtrips">
    <% @fieldtrips.each do |fieldtrip| %>
      <li>
        <%= link_to fieldtrip.Name, fieldtrip_path(fieldtrip) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
