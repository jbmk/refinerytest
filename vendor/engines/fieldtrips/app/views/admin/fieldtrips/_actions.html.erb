<ul>
  <% if Admin::FieldtripsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_fieldtrips_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_fieldtrip_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::FieldtripsController.sortable? and Fieldtrip.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Fieldtrips"),
                 admin_fieldtrips_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Fieldtrips"),
                 admin_fieldtrips_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>